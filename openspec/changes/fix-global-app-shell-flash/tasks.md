## 1. App entry shell

- [x] 1.1 Add a route-aware pre-mount marker and minimal workspace loading shell to the global HTML entry.
- [x] 1.2 Add bootstrap cleanup so the temporary shell is removed when React mounts.

## 2. Regression coverage

- [x] 2.1 Add tests for route detection and public-route preservation.
- [x] 2.2 Run global web typecheck, targeted tests, and production build; full suite retains two pre-existing unrelated failures.

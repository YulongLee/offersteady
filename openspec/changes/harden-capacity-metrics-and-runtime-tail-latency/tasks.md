## 1. 容量指标数据库修复

- [x] 1.1 新增幂等迁移，使快照粒度约束接受 `capacity_5m`、`hourly`、`daily` 并接入启动迁移清单
- [x] 1.2 增加迁移和峰值 upsert 回归测试，验证重复迁移、最大值单调与失败旁路
- [x] 1.3 运行容量监控聚焦测试与 OpenSpec 严格校验

## 2. Redis 事件增量读取

- [x] 2.1 增加有界 cursor-to-stream-id 索引与升级前 Stream 兼容读取
- [x] 2.2 让阻塞等待直接解码 `XREAD` 增量结果，避免等待前后完整 `XRANGE`
- [x] 2.3 增加长 Stream、游标过期、旧数据恢复、索引修剪和命令放大率测试

## 3. SSE 资源隔离与按需快照

- [x] 3.1 增加生产安全的专用事件等待执行器配置、生命周期和通用调用帮助函数
- [x] 3.2 将网页会话 SSE 与桌面截图 SSE 的阻塞等待切换到专用执行器
- [x] 3.3 让会话增量更新按事件类型刷新转写/候选并停止重读完整历史事件
- [x] 3.4 增加并发空闲 SSE、事件唤醒、快照单调和普通 API 不饥饿回归测试

## 4. 性能验收与交付

- [x] 4.1 运行聚焦后端测试并记录优化前后 Redis 命令量、事件唤醒与健康请求 P50/P95/P99
- [x] 4.2 运行后端全量测试、所有 workspace 测试/typecheck、生产构建和 OpenSpec strict 校验
- [x] 4.3 更新运行性能报告、迁移与回滚说明，明确仍需线上持续观察的指标

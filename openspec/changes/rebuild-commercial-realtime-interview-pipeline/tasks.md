## 1. 边界梳理与协议基线

- [x] 1.1 盘点桌面端、后端和网页端现有采集、发布、ASR 与转录代码，形成旧链路到新链路的替换清单
- [x] 1.2 定义带版本号的实时会话、双通道音频、确认应答、游标和错误事件协议，并建立脱敏合成夹具
- [x] 1.3 为新旧实时链路增加独立功能开关和会话级协议版本固定，确保灰度期间不会混用
- [x] 1.4 记录当前端到端延迟、断线恢复和资源占用基线，作为迁移验收对照

## 2. 可恢复运行态基础设施

- [x] 2.1 在本地与部署编排中加入 Redis、健康检查和必需配置，且不在客户端暴露服务密钥
- [x] 2.2 实现会话租约、桌面连接、角色通道、确认序号和转录事件流的 Redis 键结构与 TTL
- [x] 2.3 将可恢复运行态从进程内存迁移到 Redis，并保留 PostgreSQL 中的用户、面试和允许持久化的转录元数据
- [x] 2.4 实现重复连接、重复事件和服务重启后的幂等恢复逻辑

## 3. macOS 原生双通道采集

- [x] 3.1 将 Swift 原生层设为麦克风和系统输出的唯一生产采集所有者，Electron 仅负责控制和状态展示
- [x] 3.2 实现候选人麦克风与面试官系统输出的独立标准化音频通道，并携带角色、序号和时间戳
- [x] 3.3 实现耳机插拔、默认设备变化、休眠唤醒及权限变化后的自动重建，避免要求用户重新绑定面试
- [x] 3.4 为每个通道实现最多 2 秒的内存环形缓冲和明确的溢出丢弃策略，禁止落盘原始音频
- [x] 3.5 移除或隔离 Electron 主进程、渲染进程及浏览器中的重复采集和隐式回退路径
- [x] 3.6 固定 bundle identifier、签名与权限声明，并将桌面身份和令牌迁移到 Keychain

## 4. 单连接实时音频传输

- [x] 4.1 实现后端实时网关的认证 WebSocket 握手、协议协商和面试会话授权
- [x] 4.2 在一个桌面长连接中复用麦克风与系统输出通道，并实现序号、ACK 和缺口事件
- [x] 4.3 实现桌面端有界发送队列、单飞重连、指数退避及从最后确认序号恢复
- [x] 4.4 实现网关侧有界入口队列、背压、速率限制和单会话连接数量限制
- [x] 4.5 更新生产反向代理的 WebSocket 转发、超时和连接升级配置

## 5. 持久化双角色 ASR 会话

- [x] 5.1 定义可替换的实时 ASR 适配器接口，并实现 Qwen 实时 ASR 连接生命周期
- [x] 5.2 为候选人和面试官各维护一个持久 ASR 会话，将连续音频追加到对应角色连接
- [x] 5.3 使用服务端 VAD 或完整话语边界提交识别，禁止为每个短帧创建请求或强制提交
- [x] 5.4 统一部分结果、最终结果、修订结果和提供商错误为带角色与序号的内部转录事件
- [x] 5.5 实现 ASR 断线重连、有限重放和不可恢复缺口提示，且不重复发布已确认文本

## 6. 网页实时转录交付

- [x] 6.1 实现基于 Redis 事件流的转录订阅接口及按游标读取的会话快照接口
- [x] 6.2 网页面试端按游标消费、去重和有序合并部分及最终转录事件
- [x] 6.3 实现网页刷新、网络切换和短暂断线后的自动重连、游标续传与快照兜底
- [x] 6.4 以服务端权威状态机展示绑定、采集、传输、识别和交付状态，避免误报“已连接”
- [x] 6.5 清理网页在线心跳对桌面媒体发布的错误门控，仅保留授权和可观测用途

## 7. 可观测性、隐私与资源保护

- [x] 7.1 为采集、排队、上传、网关、ASR 和网页交付生成同一 trace 下的阶段耗时
- [x] 7.2 增加活动连接、ASR 会话、队列深度、丢帧、重连、文件描述符和内存指标
- [x] 7.3 建立脱敏诊断日志，禁止记录原始音频、完整转录、访问令牌及用户敏感资料
- [x] 7.4 配置资源上限、异常告警和实时服务 readiness 检查，避免故障扩散到普通问答与截图链路
- [x] 7.5 更新隐私与数据保留配置，使转录默认不持久化或仅按用户明确授权的期限保存

## 8. 自动化验证与 AI 评测

- [x] 8.1 增加双通道采集、设备切换、权限失败和缓冲溢出的桌面端回归测试
- [x] 8.2 增加乱序、丢包、重复包、背压、鉴权失败和断线续传的传输协议测试
- [x] 8.3 增加 ASR 持久会话、角色隔离、VAD 边界、重连去重和提供商故障的适配器测试
- [x] 8.4 增加后端重启、Redis 短暂不可用、网页刷新及桌面重连的端到端恢复测试
- [x] 8.5 使用合成双人音频补充 `ai/evals/`，评测角色归属、文本完整性、空转录抑制和建议触发行为
- [ ] 8.6 执行 30 分钟持续会话、并发会话与资源泄漏测试，确认连接、任务、内存和文件描述符保持有界
- [ ] 8.7 生成分阶段性能报告，验证转录 p95 不超过 2 秒、控制事件 p95 不超过 500 毫秒、短暂中断 5 秒内恢复

## 9. 灰度迁移与发布

- [ ] 9.1 在内部环境启用新链路影子模式，对比新旧转录但只向用户交付一条权威结果
- [ ] 9.2 按用户或会话灰度启用新链路，并验证旧会话可继续使用其固定协议版本
- [ ] 9.3 完成权限、耳机切换、30 分钟稳定性、后端重启和网页重连的发布验收清单
- [ ] 9.4 达到性能与稳定性门槛后删除 HTTP 逐帧发布、重复采集和进程内权威会话状态
- [x] 9.5 更新部署、故障排查、桌面签名公证、隐私说明和回滚操作文档
- [ ] 9.6 严格校验 OpenSpec 变更并逐项更新任务状态，确认全部能力场景具有实现和验证证据

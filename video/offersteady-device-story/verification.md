# 成片验证

- TypeScript：在独立视频目录执行 `node_modules/.bin/tsc --noEmit`，通过。
- 最终渲染：1470 帧，1920×1080，30fps，H.264 + AAC；ffprobe 容器时长 49.045333 秒（含音频编码尾帧），文件 7,538,479 字节。
- 逐镜抽帧：100、280、560、740、960、1160、1290、1430；检查全部八镜头，见 out/qa/final-contact.jpg。修正开场设备越界、连接码框错位和回答放大裁切。
- 连接步骤：真实输入框输入合成码 628391，真实验证按钮触发 fixture adapter，连接完成截图显示“本场设备已连接”。
- 实时页面：真实开始按钮触发后显示“正在收音”，手机对话/回答布局以及平板、电脑布局来自当前源码。采集模拟接口，无实际音频采集或后端联调。
- 双端表述：电脑助手与一个移动网页共存；平板和电脑网页分别展示，不呈现多个网页并发实时操作。
- 音频：ffmpeg 完整解码成功，音量扫描在 out/qa/audio-check.txt；没有旁白。
- 静音版移除音轨并复制视频流；封面从最终成片第 3 秒提取。
- 仅新增独立 video 目录，不改变功能和 OpenSpec，无业务行为变更，因此未新增产品变更或执行无关 OpenSpec 校验。

## 中文旁白版增量验证

- MiniMax 生成八段中文旁白，台词见 voiceover-script.md。生成请求成功；凭据通过隐藏输入读入，未持久化。
- 每段去除首尾空白后按 48kHz PCM 样本偏移拼入独立旁白轨。片尾语速调整为 1.054 倍，其余不变。首版混音发现 FFmpeg 补静音时间戳异常，已弃用该输出，改为确定性 PCM 拼接并重新验证。
- 最终音频、视频均 49.000 秒；H.264 1080p 30fps + AAC，6,813,765 字节。
- 旁白有效发声覆盖八个预定镜头，最后有效发声至 48.649 秒；音轨至 49 秒，未硬截尾字。见 out/qa/voice-silence.txt 和 voiceover-timing.json。
- 背景音乐自动侧链压低，混音均值 -23.9 dBFS、最大 -4.9 dBFS，未削波。见 out/qa/voice-mix-levels.txt。
- 原视频与配音版压缩视频流 SHA-256 完全一致：923b383016314f4aa1981745f6998ca897d7f5e7dfa013649233b47627b29661。
- 完整解码到 null 成功。工程文本凭据扫描无命中。
- 本轮验证包含接口、时长、镜头边界、音量和解码，不包含人工听辨逐字发音。

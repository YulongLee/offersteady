import {fixtureAdapter} from '../../apps/web/src/test-state';

export const runtimeConfig = {
  apiBaseUrl: 'http://127.0.0.1:8000',
  appEnvironment: 'capture',
};

export const interviewAppAdapter = fixtureAdapter;

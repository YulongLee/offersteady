# 百度关键词可见性行动计划

## 执行顺序

1. **立即核验（今天）**
   - 在百度搜索资源平台查看“抓取异常、索引量、抓取频次”，确认首页和 3 个重点页面没有拒绝抓取或安全拦截。
   - 在 sitemap 页面确认提交状态为成功，并保留 `https://mianshiwen.cn/sitemap.xml`；robots 已声明该 sitemap。
   - 对首页、`/features/ai-interview-assistant`、`/features/realtime-interview`、`/pricing` 做普通 URL 检查/请求收录（如平台提供该入口）。

2. **快速收益（本周）**
   - 保持首页当前标题和 h1 至少 2 周不频繁改动；当前线上标题、描述和静态 h1 已具备明确主题。
   - 在百度平台用“查询词”维度记录截图中的 8 个词，建立基线：展现、点击、CTR、平均排名。
   - 确认 sitemap 中的 30 个 URL 均为公开、可索引、返回 200；新增页面才更新 `lastmod`，不要批量制造低质量 URL。
   - 修复或替换备案外链 521，减少页面质量检查中的异常链接。

3. **战略优化（2–4 周）**
   - 围绕一个明确意图建设页面集群：
     - `/features/ai-interview-assistant`：AI 面试助手/程序员面试助手；
     - `/features/realtime-interview`：实时面试辅助/语音 AI 面试对话；
     - `/features/screenshot-answer`：截图回答；
     - `/features/interview-review`：面试复盘。
   - 每个页面补充独立的使用场景、输入方式、限制、隐私说明和真实示例，避免只替换关键词的重复页面。
   - 从首页、指南和面试题专题页使用自然锚文本链接到上述页面，形成“功能—指南—面试题”的主题网络。
   - 发布可被引用的原创内容（面试准备清单、STAR 回答、技术面试题解析等），逐步积累品牌提及和自然链接。

## 不建议做的事

- 不要把 `.env` 中的模型、接口或应用配置当作百度关键词配置；这些字段不会自动生成搜索词。
- 不要为了追求词量在首页堆砌“AI 面试助手”等重复短语，也不要短时间内反复改标题、URL 或 sitemap 日期。
- 不要用一次 `site:` 搜索失败判断未收录；百度安全验证、地域、个性化和索引延迟都会影响结果。

## 成功判定

- 7 天：重点 URL 无抓取异常，sitemap 状态正常，查询词展现量开始稳定增长。
- 14–28 天：核心长尾词平均排名持续进入前 10，CTR 出现非零数据。
- 4 周后：再决定是否扩展新词或调整页面，而不是依据单日波动。

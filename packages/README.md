# Packages

这里用于至少被两个应用复用的模块，例如 UI 组件、共享类型、AI 编排核心和统一配置。

当前第一阶段重点：

- `packages/protocol` 继续承载前端与服务端共享的协议类型
- 新增的工程底座与占位接口契约也优先放在这里沉淀
- 真实业务逻辑仍应先落在具体应用中，确认复用边界后再抽包

不要因为“以后可能复用”而提前抽包；先在实际使用位置实现，出现清晰复用边界后再迁移。
